#include <stdlib.h>


long next_prime(long bottom)
/* naive prime gen. */
{
	while(
}


int main(int argc, char *argv[])
{
	char *ptr;
	unsigned long number = (argc > 1 ? strtoul(argv[1], &ptr, 10) :  600851475143);

	return 0;
}
